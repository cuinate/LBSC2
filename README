==== how to ====
分三步：
1。 下载代码
2。 安装 rails 3.0 到本地
3. 安装 bundler , <---- gem install bundler
4. 更新gem 的依赖 关系 ，到代码的安装路径，运行 ----> bundle install
5。如果 mysql 数据库有密码，则更改 数据库配置文件
6. 删除原有数据库，run :
  rake db:create
  rake db:migrate
7. 玩了